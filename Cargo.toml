[package]
name = "differential-equations"
version = "0.1.0"
edition = "2024"
description = "A proof-of-concept port of Julia SciML ODE solvers to Rust"
publish = false

[features]
allocation-metrics = []

[dependencies]

[dev-dependencies]
stats_alloc = "0.1.10"
